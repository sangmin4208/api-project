# 1. 컴파일된 실행 파일 (OS별)
*.exe
*.exe~
*.dll
*.so
*.dylib
main
server

# 2. 환경 설정 파일 (보안 중요! 절대 올리면 안 됨)
.env

# 3. 데이터베이스 파일 (로컬 데이터는 올리지 않음)
*.db
*.sqlite
*.sqlite3

# 4. IDE 설정 파일 (VSCode, GoLand 등)
.vscode/
.idea/

# 5. OS 생성 파일
.DS_Store
Thumbs.db